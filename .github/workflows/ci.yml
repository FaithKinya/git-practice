name: Simple CI

on:
  push:
    branches: [ master ]

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run Python script
        run: python3 app.py
